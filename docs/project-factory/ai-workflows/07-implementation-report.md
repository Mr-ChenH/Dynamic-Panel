# AI 整理 MVP 实施报告

> 实施基线：`f6e56d5` 之后的当前工作区
> 状态：核心本地闭环已实现，真实模型质量、macOS 实机与安装包验收未执行
> 验收口径：本文件只记录代码和自动化已证明的事实，不把计划或假 provider 结果写成真实模型结论。

## 1. 已交付范围

### 统一请求底座

- `ai/schema.js` 集中校验动作、请求大小、12000 UTF-16 code units 输入上限、结构化响应、最多 20 个待办和原文依据。
- `ai/prompts.js` 对摘要、精简、翻译、待办提取、录音整理和兼容自动命名使用版本化提示词。
- `ai/service.js` 统一 OpenAI-compatible 请求、30 秒总超时、DNS 阶段起即可取消、供应商错误映射、并发边界、文本 SSE 增量，以及 256 KiB 流式响应读取上限。SSE 必须收到 `[DONE]` 或正常的 `finish_reason: stop`；畸形数据帧、`length` 截断、内容过滤和其他非正常结束均不能作为成功结果保存。普通 JSON 响应同样检查结束原因。
- `main.js` 只接受白名单结构，通过 `ai:run`、`ai:cancel`、`ai:test-provider` 暴露能力；渲染进程销毁、工作区切换或 AI 配置保存时取消活动与排队请求。远程 HTTPS 请求固定使用已经过公网检查的 DNS 地址，避免请求时二次解析绕过。
- 旧 `smart:organize-material` 和启用后的链接智能命名继续可用，但都改由统一服务执行；笔记、录音和链接自动命名均携带稳定 source ID 与内容 revision，再按来源合并并进入最多 10 项的有界队列。交互请求保持优先；队列等待最多 30 秒，并绑定入队时的工作区/配置代次，过期或上下文变化后不再发送。取消中的底层请求在真正结算前继续占用并发槽位。

### 用户工作流

- 启动器提供“从文字提取待办 / 摘要文字 / 精简文字 / 翻译文字”四个显式命令。
- 待办页提供“从文字添加”，生成后逐条编辑标题、责任领域和截止时间，并可复用现有规则选择“今天 / 本周 / 以后”，再一次性写入原 `notch-todo-data`。
- 缺少、无效、已过去的截止时间不能保存；待办必须保留能在原文中定位的摘录。
- 批量保存具有当前会话幂等保护和冲突安全撤销：用户保存后继续修改的待办不会被撤销误删。
- 笔记详情提供“整理笔记”和“生成标题”；选区存在时仅发送选区，精简和翻译结果可以替换选区并撤销，选区包含 Markdown 图片时不提供直接替换。选区位置校验保留未裁剪原文，首尾空白不会造成错误 revision；生成标题先预览和编辑，再显式采用。
- 录音详情提供“整理录音”和“生成名称”；链接条目提供智能名称/分类动作。名称和分类都先预览，采用前校验来源与旧名称，并支持冲突安全撤销；链接分类复用现有分组。
- 录音整理只发送已保存的转写文字，音频文件不进入模型请求。摘要可另存为普通 Markdown 笔记，行动项仍需补齐并确认后加入待办。
- AI 工作台支持停止请求、关闭或切换工作区时取消、来源变化检查、复制结果和明确错误状态；生成期间锁定输入、动作和流式结果，完整结束后才开放编辑。新会话会重置请求控件，旧响应不能污染重开的工作台；保存、采用和撤销使用会话级互斥，异步返回后再次校验会话。服务主机、来源范围、参考时间及时区均在请求前显示。普通搜索与正则搜索不会触发模型。
- AI 写入会区分 LocalStorage 成功与工作区快照同步失败；同步失败时保留已写入的本机数据并明确提示，由既有周期同步继续重试，不会重复追加。

### 配置与隐私

- “设置 → AI 与转写”增加 LLM 超时、文本加结构化输出连接测试、最近 50 条脱敏请求诊断，以及笔记、录音、链接三个独立自动命名开关。诊断包含独立的排队等待和模型请求耗时，`queue_full`、`queue_expired` 与排队上下文失效也会留下不含正文的记录。
- 三个自动调用开关默认关闭。已有 Base URL、模型和加密 Key 保留，但不会因迁移自动开启内容发送；旧配置首次显示一次迁移说明，确认或保存后写入版本标记。
- API Key 继续由主进程 `safeStorage` 或环境变量解析，不返回渲染层、不进入 LocalStorage 和工作区导出。
- 新请求只发送用户当前明确选择的文字、分类显示名、当前时间和时区；模型输出不能指定 IPC、Shell、文件路径、LocalStorage key 或永久对象 ID。

## 2. 数据兼容

- 正式待办继续写入 `notch-todo-data` 的根级 `P0` 至 `P3`，没有引入第二套正式任务库。
- AI 摘要继续使用 `notch-note-archive-v1` 创建普通 Markdown 笔记。
- 候选、原文引用、请求状态和撤销令牌只存在于当前渲染会话，不写入工作区。
- `package.json` 已将 `ai/**/*.js` 加入桌面构建文件清单；本轮按用户约束没有执行打包。

## 3. 自动化证据

当前 `npm test` 结果：

- Node 测试：170 通过，1 项因当前 Windows 平台跳过，0 失败。
- Electron：`notch-focus.electron.js`、`retained-workspace.electron.js`、`startup.electron.js` 全部通过。
- AI 单元覆盖请求/schema 限制、证据校验、DNS 校验阶段取消、取消槽位竞争、并发限制、不同来源自动命名、队列满/过期与绑定漂移诊断、全量取消、事件投递异常隔离、SSE 增量组装、畸形帧/断流/截断结束拒绝、超大响应拒绝、相似/批内重复、待办批量写入与冲突撤销。
- 生产 Electron 场景使用固定假 provider 验证：迁移说明、无配置错误、手动旧来源拒绝、流式结果只读、生成中动作锁定、关闭重开控件恢复、重复保存互斥、保存中关闭、全文首尾空白、带首尾换行的选区替换/撤销、文字提取候选、日期审阅、一次写入、同步失败提示、录音摘要，以及笔记/录音/链接手动命名与撤销。
- 生产主进程诊断路径以 52 个被安全策略拒绝的 loopback 请求验证：磁盘与界面只保留最近 50 条，复制内容不包含正文、Key 或完整 URL，清空后文件读取为空；测试不向公网发送请求。
- UI 截图保存在被 Git 忽略的 `dist.noindex/ai-workspace-review.png`，用于本地视觉检查。

测试中的 `127.0.0.1:43821 EADDRINUSE` 是既有完成提醒端口被另一个本机进程占用的非阻塞提示，不影响测试结果。

## 4. 尚未证明或未纳入本轮

- 未使用真实 DeepSeek 或其他供应商执行 `06-validation-plan.md` 的 60 条质量样本，因此不能报告精确率、召回率或日期理解质量。
- 未执行 macOS 实机、Windows 10 实机、物理多屏和安装包内请求验证。
- 首版仍不包含跨重启草稿、长内容自动分段、工作区问答、向量索引、Ollama、本机系统上下文或网页全文抓取。
- 当前日期候选由模型提出、由客户端规则校验为未来合法时间；无法确定日期时依赖用户在预览中补齐，没有自动追问对话。
- 供应商网络仍遵循应用现有 HTTPS 公网配置模型，不提供企业级 DLP、代理审计或完整网络隔离；请求会拒绝解析为本机/内网的地址并固定本次公网 DNS 结果。

## 5. 后续验收顺序

1. 使用脱敏固定样本运行真实供应商质量评测，单独记录模型、参数、时间和失败样本。
2. 在 macOS 13+ 与 Windows 10/11 实机验证中文输入法、停止/关闭、睡眠恢复、离线和系统代理场景。
3. 获得明确打包授权后，再验证安装包中的 `ai/**/*.js`、`safeStorage`、环境变量和升级迁移。
4. 质量门槛和实机验收通过后，才将本文件状态改为“完整 MVP 验收通过”。
